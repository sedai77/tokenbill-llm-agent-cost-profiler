### blocks.breakpoints — breakpoint placement

Place prompt-cache breakpoints after the static prefix and at the end of the conversation
(`static_plus_end`), or every 15 blocks for long tool loops (`every_15`), within the provider's
limit of four breakpoints. Docs:
https://platform.claude.com/docs/en/build-with-claude/prompt-caching
