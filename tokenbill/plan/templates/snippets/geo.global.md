### geo.global — drop the US-only inference geography where allowed

This is a policy decision, not a setting: requests pinned to a US-only inference geography pay a
premium. Where your data-residency policy allows it, stop requesting the US-only geography in the
SDK or gateway configuration. Get residency approval first.
