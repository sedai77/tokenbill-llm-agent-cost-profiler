### batch.eligible — move non-interactive calls to batch processing

CI, evaluation, scheduled and other non-interactive calls that can wait for results are eligible
for the Message Batches API at batch prices. Submit them as batches from the SDK. Docs:
https://platform.claude.com/docs/en/build-with-claude/batch-processing
