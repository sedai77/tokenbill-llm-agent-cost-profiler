### endpoint.global — global instead of regional endpoints

Regional endpoints on cloud channels carry a premium over global ones. Where residency rules allow,
switch the gateway or SDK client configuration to the global endpoint. Get residency approval
first.
