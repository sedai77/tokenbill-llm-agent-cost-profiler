"""Source adapters (SPEC §5): each implements core.protocols.Adapter and is registered by dotted
path in core.registry.BUILTIN_ADAPTERS.

Docstring-only package marker owned by F-CORE (SPEC §2.2, §21 #2).
"""
