"""GitHub Copilot extension (SPEC-v0.2-COPILOT): rate data and verifier, live pulls, the admin
handoff kit, record store and context enricher, reconciler, aggregate plan, policy pack, summary,
section renderer, showback and FOCUS rows. Reached only through ``core.registry.EXTENSIONS`` and
``core.extensions``; each module belongs to its Copilot package (OWNERSHIP.toml).

Docstring-only package marker owned by F-CORE (SPEC §2.2, §21 #2; CORE-AMENDMENTS C-31).
"""
